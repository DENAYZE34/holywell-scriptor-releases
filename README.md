# HOLYWELL Scriptor — releases

This tiny public repo holds only `version.json`, checked by the plugin
itself to notify you when a newer build is available. It does not contain
any plugin source code.
